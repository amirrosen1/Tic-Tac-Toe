/**
 * A class that implements Renderer, but is an empty implementation of Renderer, that doesn't display
 * anything on the screen.
 */
public class VoidRenderer implements Renderer {
    @Override
    public void renderBoard(Board board) {

    }
}